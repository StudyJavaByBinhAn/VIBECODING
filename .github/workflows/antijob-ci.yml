name: antijob CI

on:
  push:
    branches: [main, develop]
    paths:
      - "VIBECODING/antijob/**"
      - ".github/workflows/antijob-ci.yml"
  pull_request:
    paths:
      - "VIBECODING/antijob/**"
      - ".github/workflows/antijob-ci.yml"

jobs:
  build:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: VIBECODING/antijob/antijob

    services:
      postgres:
        image: postgres:16
        env:
          POSTGRES_DB: dental_db
          POSTGRES_USER: postgres
          POSTGRES_PASSWORD: postgres
        ports:
          - 5432:5432
        options: >-
          --health-cmd "pg_isready -U postgres -d dental_db"
          --health-interval 5s
          --health-timeout 5s
          --health-retries 10

      redis:
        image: redis:7
        ports:
          - 6379:6379
        options: >-
          --health-cmd "redis-cli ping"
          --health-interval 5s
          --health-timeout 5s
          --health-retries 10

    steps:
      - uses: actions/checkout@v4

      - name: Set up JDK 21
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: "21"

      - name: Grant execute permission for gradlew
        run: chmod +x gradlew

      - name: Build + test (bao gồm Flyway migration thật trên Postgres service container, @DataJpaTest qua Testcontainers, Jacoco coverage gate, SpotBugs static analysis)
        env:
          DB_HOST: localhost
          DB_PORT: 5432
          DB_NAME: dental_db
          DB_USERNAME: postgres
          DB_PASSWORD: postgres
          REDIS_HOST: localhost
          REDIS_PORT: 6379
        run: ./gradlew build --console=plain

      - name: Publish test report
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: junit-test-report
          path: VIBECODING/antijob/antijob/build/reports/tests/test
          retention-days: 14

      - name: Publish Jacoco coverage report
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: jacoco-coverage-report
          path: VIBECODING/antijob/antijob/build/reports/jacoco/test/html
          retention-days: 14

      - name: Publish SpotBugs report
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: spotbugs-report
          path: VIBECODING/antijob/antijob/build/reports/spotbugs
          retention-days: 14
